# -*- coding: utf-8 -*-
"""machine_failure_deployment.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/13gEYi5d-yxm9H5h0tUXmyhiTZ0WU6HCq
"""

import streamlit as st
import pandas as pd
import joblib

model = joblib.load('machine_failure_prediction_model.pkl')

st.title('Machine Failure Prediction system')

st.write("you can enter the data sensor and check whether your machine will work or fall under certain condition")

footfall = st.number_input('footfall')
tempMode = st.number_input('tempMode')
AQ = st.number_input('AQ')
USS = st.number_input('USS')
Cs = st.number_input('Cs')
VOC = st.number_input('VOC')
RP = st.number_input('RP')
IP = st.number_input('IP')
Temperature  = st.number_input('Temperature')

df = pd.DataFrame({
    "footfall":[footfall],
    "temMode": [tempMode],
    "AQ": [AQ],
    "USS": [USS],
    "Cs": [Cs],
    "VOC" : [VOC],
    "RP" : [RP],
    "IP" : [IP],
    "Temperature" : [Temperature]
})

if st.button('Predict'):
  prediction = model.predict(df)
  if prediction[0] == 1:
    st.error('Machine will fall')
  else:
    st.success('Machine will work')